---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { useTranslations, type Lang } from "../../i18n/utils.ts";
import "../../styles/global.css";

const lang = "zh" as const satisfies Lang;
const t = useTranslations(lang);
---

<BaseLayout
  lang={lang}
  title={t.termsTitle}
  description="Splity 服务条款：免费使用、按现状提供、不承担责任。"
>
  <Header lang={lang} />
  <main>
    <section class="section">
      <div class="container container-narrow">
        <span class="section-eyebrow">{t.termsEyebrow}</span>
        <h1>{t.termsTitle}</h1>
        <p class="updated">{t.termsUpdated}</p>

        <div class="doc">
          {t.termsSections.map((s) => (
            <>
              <h2>{s.h}</h2>
              <p>{s.p}</p>
            </>
          ))}
        </div>
      </div>
    </section>
  </main>
  <Footer lang={lang} />
</BaseLayout>

<style>
  .container-narrow { max-width: 720px; }
  h1 { margin-bottom: 8px; }
  .updated { color: var(--text-muted); font-family: var(--font-mono); font-size: 0.9rem; margin-bottom: 40px; }
  .doc h2 { font-size: 1.3rem; margin: 32px 0 12px; }
  .doc p { margin-bottom: 16px; line-height: 1.8; color: var(--text-secondary); }
</style>
